# Core Framework & API
django
strawberry-graphql[django,asgi] # <-- UPDATED
channels                 # For ASGI routing

# Production Server & Static Files
gunicorn
uvicorn                  # Required worker for Gunicorn to run ASGI
whitenoise               # For serving static files efficiently

# Database & Settings
psycopg2-binary          # For connecting to PostgreSQL
dj-database-url          # For parsing database URLs from Render's environment
python-decouple          # For managing environment variables

# Optional Packages (kept from your original list)
django-filter            # Useful for filtering querysets
text-unidecode           # You may need this for specific text processing