# Core Framework & API
django
strawberry-graphql[asgi]      # For the ASGI server support
strawberry-graphql-django   # Explicitly add the Django integration
channels                    # For ASGI routing

# Production Server & Static Files
gunicorn
uvicorn
whitenoise

# Database & Settings
psycopg2-binary
dj-database-url
python-decouple

# Optional Packages
django-filter
text-unidecode